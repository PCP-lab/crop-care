# --- API & server ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-multipart
python-dotenv        # for .env handling (optional but useful)

# --- ML / CV / NLP (without torch; install CUDA torch separately) ---
ultralytics>=8.1.0   # YOLOv8
transformers>=4.40.0
open-clip-torch
pillow
numpy
pandas
requests

# --- Optional utilities ---
tqdm
